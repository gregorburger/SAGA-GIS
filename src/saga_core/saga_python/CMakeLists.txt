include_directories(${PYTHON_INCLUDE_PATH})

SET_SOURCE_FILES_PROPERTIES(saga.i PROPERTIES CPLUSPLUS ON)
#SET_SOURCE_FILES_PROPERTIES(example.i PROPERTIES SWIG_FLAGS "-includeall")
SWIG_ADD_MODULE(saga python saga.i)
SWIG_LINK_LIBRARIES(saga saga_api ${PYTHON_LIBRARIES})
